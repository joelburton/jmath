LDLIBS = `pkg-config --libs glib-2.0`
CFLAGS = -Wall -g `pkg-config --cflags glib-2.0`

all: math test

math: math.o add.o sub.o

test: test.o add.o sub.o

docs:
	doxygen Doxyfile

.PHONY: clean
clean:
	rm -rf *.o core math test html/ latex/
